const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../viewpost';

describe('posts.actions.viewpost', () => {
  it('should do something');
});
